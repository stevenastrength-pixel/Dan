# Development
DATABASE_URL="file:./dev.db"

# Docker (set automatically by docker-compose)
# DATABASE_URL="file:/app/data/novel.db"

# Auth — generate with: node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
JWT_SECRET="change-this-to-a-long-random-secret-before-deploying"

# Invite codes are managed in-app by admins (Settings → Admin panel)
